#code
def isSorted(a, n):
    for i in range(1, n):
        if a[i] < a[i - 1]:
            return False
    return True
n= int(input())
a=[]
for i in range(n):
    temp=int(input())
    a.append(temp)

print("True" if(isSorted(a,n)) else "False")
#print(isSorted(a,n))

#O(n), O(1)
